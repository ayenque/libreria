<%= link_to("<< Volver a libros", books_path(:author_id => @author.id)) %>

<h2> Nuevo libro</h2>

<%= form_for(@book, :url => books_path(:author_id => @author.id)) do |f| %>
	
	<%= render( :partial => "form", :locals => { :f => f }) %>

	<div>
		<%= f.submit("Crear libro") %>
	</div>

<% end %>